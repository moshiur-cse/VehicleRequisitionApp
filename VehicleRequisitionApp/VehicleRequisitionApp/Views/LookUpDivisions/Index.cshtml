@model IEnumerable<VehicleRequisitionApp.Models.LookUpDivision>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DivShortName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DivFullName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DivShortName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DivFullName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DivisionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DivisionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DivisionId })
        </td>
    </tr>
}

</table>
