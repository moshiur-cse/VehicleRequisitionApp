@model IEnumerable<VehicleRequisitionApp.Models.LookUpManagementType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SortingSerialNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ManagementType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SortingSerialNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ManagementType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ManagementTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ManagementTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ManagementTypeId })
        </td>
    </tr>
}

</table>
