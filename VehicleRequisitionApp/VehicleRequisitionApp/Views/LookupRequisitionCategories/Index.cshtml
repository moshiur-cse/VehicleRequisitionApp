@model IEnumerable<VehicleRequisitionApp.Models.LookupRequisitionCategory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.RequisitionCategory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.RequisitionCategory)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RequisitionCategoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RequisitionCategoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RequisitionCategoryId })
        </td>
    </tr>
}

</table>
