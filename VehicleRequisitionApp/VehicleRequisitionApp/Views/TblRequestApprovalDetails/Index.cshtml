@model IEnumerable<VehicleRequisitionApp.Models.TblRequestApprovalDetail>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RequisitionId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ApprovalAuthorityId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ApprovalStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comments)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RequisitionId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ApprovalAuthorityId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ApprovalStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comments)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RequestApprovalDetailId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RequestApprovalDetailId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RequestApprovalDetailId })
        </td>
    </tr>
}

</table>
